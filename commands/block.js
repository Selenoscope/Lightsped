<client>.command({
    module.exports = ({
       name: "block",
       code: `**Sending a block request for $message" $channelSendMessage[1231062812244054026;Hello!;false]` // The ping function returns the current ping.
   });